#
# Shared configuration for AoC puzzle modules
#

dependencies:
  - $libs.kotlinx.coroutines.core
  - ../utilities/utils

settings:
  kotlin:
    powerAssert:
      enabled: true
      functions:
        - kotlin.assert
        - kotlin.require
        - kotlin.check